name: AOO Server — Build on Linux

on:
  push:
    branches:
  pull_request:

jobs:
  build:
    name: Build Linux
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libcurl4-openssl-dev pkg-config

      - name: Build
        run: |
          cd Builds/LinuxMakefile
          CONFIG=Release make

      - name: Upload Sonobus_artefacts
        uses: actions/upload-artifact@v4
        with:
          name: AOO-server-${{ runner.os }}
          path: |
            Builds/LinuxMakefile/build/aooserver

